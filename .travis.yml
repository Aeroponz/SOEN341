
language: php

php:
  - 7.0
  - 7.1

services:
  - mysql

addons:
  apt:
    sources:
      - mysql-5.7-trusty
    packages:
      - mysql-server

dist: trusty

sudo: required

before_script:
  - mysql -e 'create database antvel;'

script: phpunit $TRAVIS_BUILD_DIR/src/tests/UnitTest.php